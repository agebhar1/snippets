---
name: jdbc

services:
  # https://hub.docker.com/r/gvenzl/oracle-free
  oracle:
    image: gvenzl/oracle-free:23-slim
    ports:
      - "127.0.0.1:1521:1521"
    environment:
      APP_USER: jdbc
      APP_USER_PASSWORD: jdbc
      ORACLE_DATABASE: demo
      ORACLE_RANDOM_PASSWORD: yes
